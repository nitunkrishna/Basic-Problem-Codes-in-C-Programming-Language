#include<stdio.h>
int add(int *arr, int n);

int main()
{
    int arr[3] = {4, 5, 6};  
    int result = add(arr, 3);
    printf("Answer = %d\n", result);
}

int add(int *arr, int n)
{
    int sum = 0;
    for(int i=0; i<n; i++)
        sum+= arr[i];
    return sum;
}
